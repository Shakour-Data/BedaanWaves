apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-ml-pv
  labels:
    app: bedaanwaves
    tier: database
spec:
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: standard
  hostPath:
    path: /data/postgresql/ml
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-ml-pvc
  labels:
    app: bedaanwaves
    tier: database
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: standard
  selector:
    matchLabels:
      app: bedaanwaves
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-ml
  labels:
    app: bedaanwaves
    tier: database
    db: ml
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: bedaanwaves
      tier: database
      db: ml
  template:
    metadata:
      labels:
        app: bedaanwaves
        tier: database
        db: ml
    spec:
      containers:
      - name: postgres
        image: postgres:17-alpine
        ports:
        - containerPort: 5432
          name: postgres
        env:
        - name: POSTGRES_DB
          value: "bedaanwaves_ml"
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: bedaanwaves-db-secrets
              key: postgres-user
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: bedaanwaves-db-secrets
              key: postgres-password
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - bedaanwaves
            - -d
            - bedaanwaves_ml
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 5
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - bedaanwaves
            - -d
            - bedaanwaves_ml
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 3
        startupProbe:
          exec:
            command:
            - pg_isready
            - -U
            - bedaanwaves
            - -d
            - bedaanwaves_ml
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 30
      volumes:
      - name: postgres-storage
        persistentVolumeClaim:
          claimName: postgres-ml-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-ml
  labels:
    app: bedaanwaves
    tier: database
    db: ml
spec:
  selector:
    app: bedaanwaves
    tier: database
    db: ml
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  type: ClusterIP
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-ml-config
data:
  postgres.conf: |
    max_connections = 150
    shared_buffers = 256MB
    effective_cache_size = 1GB
    work_mem = 16MB
    maintenance_work_mem = 64MB
    wal_level = replica
    max_wal_senders = 5
    wal_keep_size = 512MB
    random_page_cost = 1.1
    seq_page_cost = 1.0
    effective_io_concurrency = 200
    log_statement = 'ddl'
    log_min_duration_statement = 2000
    shared_preload_libraries = 'pg_stat_statements, pg_cron'