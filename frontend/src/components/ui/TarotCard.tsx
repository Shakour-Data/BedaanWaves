import type { ReactNode } from "react";
import { cn } from "@/lib/cn";

export function TarotCard({
  children,
  icon,
  title,
}: {
  children: ReactNode;
  icon?: ReactNode;
  title?: string;
  className?: string;
}) {
  return null;
}
